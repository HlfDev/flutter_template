name: app
description: "App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  # Shared Packages
  core:
    path: ../core
  localization:
    path: ../localization
  design_system:
    path: ../design_system


  # Feature Packages
  post:
    path: ../../features/post


dev_dependencies:
  flutter_test:
    sdk: flutter
